
@{
}

<div class="mx-auto rounded border p-4" style="width:420px">
    <h2 class="text-center mb-3">Order Details</h2>
    <hr />

    @if (ViewBag.Success != null)
    {
        <div class="alert alert-success" role="alert">@ViewBag.Success</div>
        <a class="btn btn-outline-primary" asp-controller="ClientOrders" asp-action="Index">Back</a>
    }
    else
    {
        <div class="row mb-3">
            <div class="col-5">Delivey Address</div>
            <div class="col-7">@ViewBag.DeliveryAddress </div>

        </div>

        <div class="row mb-3">
            <div class="col-5">Payment Method</div>
            <div class="col-7">@ViewBag.PaymentMethod</div>

        </div>

        <div class="row mb-3">
            <div class="col-5">TotalAmount</div>
            <div class="col-7">@ViewBag.TotalAmount</div>

        </div>

        <div class="row mb-3">
            <div class="col-5">CartSize</div>
            <div class="col-7">@ViewBag.cartSize</div>

        </div>

        <form method="post">
            <div class="row">
                <div class="col-sm-6 d-grid">
                    <button type="submit" class="btn btn-primary">Confirm</button>
                </div>

                <div class="col-sm-6 d-grid">
                    <a class="btn btn-outline-primary" asp-controller="Cart" asp-action="Index">Back</a>
                </div>
            </div>

        </form>

    }

    

</div>
